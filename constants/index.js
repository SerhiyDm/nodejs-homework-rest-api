const { dataRegexpEmail, dataRegexpPhone } = require("./regex");
const subscriptionValues = require("./subscriptionValues");

module.exports = {
  dataRegexpEmail,
  dataRegexpPhone,
  subscriptionValues,
};
